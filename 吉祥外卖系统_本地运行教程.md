# 「吉祥俄中餐厅」外卖系统 —— 本地保存与运行教程

本教程带您一步步把项目代码保存到本地电脑，并在本地完整运行（前端 + 后端 + 数据库）。

全程预计 20–40 分钟（取决于网络与是否已装过数据库）。

---

## 第 0 步：了解项目结构

| 目录 / 文件 | 作用 |
| --- | --- |
| `src/` | 前端页面（React + Tailwind，顾客端 + 管理后台） |
| `api/` | 后端接口（Hono + tRPC：注册登录、菜单、订单、支付预留） |
| `db/schema.ts` | 数据库表结构定义（Drizzle ORM） |
| `db/seed.ts` | 种子数据脚本（管理员账号 + 29 道菜品 + 7 个标签） |
| `dist/` | 构建产物（本地重新构建即可，可不下载） |
| `node_modules/` | 依赖包（**不要下载**，本地重新安装） |
| `.env` | 环境变量（含云端数据库密码，**不要外泄**） |

> 本项目用的是「自有邮箱/密码登录」，不依赖平台账号体系，所以导出后可以完全独立运行，只需准备一个 MySQL 数据库。

---

## 第 1 步：导出项目代码到本地

1. 在本对话中找到网站的**版本卡片**（吉祥俄中餐厅外卖系统全栈版）。
2. 点击卡片上的**「下载代码 / 导出」**按钮，得到一个 zip 压缩包。
3. 在本地解压到您喜欢的位置，例如：
   - Windows：`D:\projects\jixiang-app`
   - macOS / Linux：`~/projects/jixiang-app`
4. 如果压缩包里包含 `node_modules/` 文件夹，**直接删除它**（体积大且与您电脑系统不一定匹配，后面会重装）。

> 下文统一以 `~/projects/jixiang-app` 作为项目目录，Windows 用户请自行替换成您的路径；Windows 建议使用 PowerShell 或 Git Bash 执行命令。

---

## 第 2 步：安装 Node.js（如已安装可跳过）

1. 打开 <https://nodejs.org>，下载 **Node.js 20 LTS**（或更高版本）并安装。
2. 安装完成后打开终端（PowerShell / Terminal），验证：

```bash
node -v    # 应显示 v20.x.x 或更高
npm -v     # 应显示 10.x.x 或更高
```

---

## 第 3 步：准备一个 MySQL 数据库

项目需要一个 MySQL 8 数据库。三种方式任选其一：

### 方式 A：Docker（最省事，推荐）

已安装 Docker 的话，一条命令启动一个 MySQL：

```bash
docker run -d --name jixiang-mysql \
  -e MYSQL_ROOT_PASSWORD=root123456 \
  -e MYSQL_DATABASE=jixiang \
  -p 3306:3306 \
  mysql:8
```

对应的连接串（第 4 步要用）：

```
mysql://root:root123456@localhost:3306/jixiang
```

### 方式 B：本地安装 MySQL

从 <https://dev.mysql.com/downloads/> 安装 MySQL 8，然后创建一个数据库：

```sql
CREATE DATABASE jixiang DEFAULT CHARACTER SET utf8mb4;
```

连接串格式：`mysql://用户名:密码@localhost:3306/jixiang`

### 方式 C：云数据库

使用您已有的云 MySQL（阿里云 RDS、TiDB Cloud 等），拿到连接串即可。
如果连接串要求 SSL，请使用 `?ssl={"rejectUnauthorized":true}` 结尾的形式。

---

## 第 4 步：配置环境变量

1. 在项目根目录找到 `.env` 文件（导出的代码里带有）。
2. 用文本编辑器打开，把 `DATABASE_URL` 改成您**本地数据库**的连接串：

```bash
APP_ID=local-dev
APP_SECRET=local-dev-secret
VITE_APP_ID=local-dev

# 改成您第 3 步准备的连接串（方式 A 的示例如下）
DATABASE_URL=mysql://root:root123456@localhost:3306/jixiang
```

> ⚠️ 注意：导出包里的 `DATABASE_URL` 指向的是平台上的云端数据库。改成您自己的库之后，数据就从零开始（菜单会在第 6 步重新填充）。`.env` 不要提交到公开仓库。

---

## 第 5 步：安装依赖并初始化数据库

在项目目录下打开终端，依次执行：

```bash
# 1. 进入项目目录
cd ~/projects/jixiang-app

# 2. 安装依赖（约 1–3 分钟）
npm install

# 3. 把表结构同步到数据库（自动建 9 张表）
npm run db:push
# 如果中途询问是否确认执行，输入 y 回车

# 4. 填充种子数据：管理员账号 admin/admin123 + 7 个标签 + 29 道菜品
npx tsx db/seed.ts
```

看到 `Done.` 说明初始化完成。

---

## 第 6 步：构建并启动

### 生产模式（推荐，和线上效果一致）

```bash
npm run build   # 构建前端 + 打包后端，输出到 dist/
npm start       # 启动服务器
```

看到 `Server running on http://localhost:3000/` 即成功。

### 开发模式（改代码自动热更新）

```bash
npm run dev
```

同样访问 <http://localhost:3000>。

---

## 第 7 步：验证系统

浏览器访问 <http://localhost:3000>：

| 验证项 | 操作 | 预期 |
| --- | --- | --- |
| 菜单展示 | 打开首页 | 看到 6 大分类 29 道菜品，可按标签筛选 |
| 注册登录 | 点「登录 / 注册」→ 注册标签页 | 邮箱注册成功并自动登录 |
| 下单 | 加购 → 购物车 → 选「外卖送上门」填地址 → 提交 | 提示下单成功，生成订单号 |
| 付款 | 我的订单 → 去付款 → 我已完成付款 | 付款状态变「已付款」 |
| 管理后台 | 访问 `http://localhost:3000/admin`，账号 `admin` / `admin123` | 看到订单管理页和刚才的订单 |
| 接单流转 | 确认接单 → 开始配送 → 完成 | 状态依次流转，顾客端同步更新 |
| 菜单管理 | 管理后台 → 菜单管理 | 可改价、传图、设标签、上下架 |

---

## 常见问题排查

| 现象 | 原因与解决 |
| --- | --- |
| `npm install` 报错或卡住 | 切换国内镜像：`npm config set registry https://registry.npmmirror.com` 后重试 |
| `db:push` 提示连接被拒绝 | MySQL 没启动（方式 A 检查 `docker ps`）；或连接串里的主机/端口/密码不对 |
| `db:push` 提示 `Unknown database` | 数据库还没创建，回到第 3 步创建 `jixiang` 库 |
| 打开页面空白 | 先看终端有无报错；确认访问的是 `http://localhost:3000` 而不是直接打开 html 文件 |
| 端口被占用 | 其他程序占用了 3000 端口：Windows `netstat -ano \| findstr :3000`；macOS/Linux `lsof -ti:3000 \| xargs kill` |
| 登录后刷新又退出 | 浏览器禁用了 localStorage，换 Chrome/Edge 正常模式即可 |
| 图片上传失败 | 图片超过 2MB 或格式不是 JPG/PNG/WebP/GIF |

---

## 日常使用的几条命令

```bash
npm run dev        # 开发模式（改代码热更新）
npm run build      # 构建生产版本
npm start          # 运行生产版本
npm run check      # TypeScript 类型检查
npm run db:push    # 改了 db/schema.ts 之后同步表结构
npx tsx db/seed.ts # 重新填充种子数据（已存在的菜品会跳过，不会重复）
```

---

## 安全提醒（部署到公网前必读）

1. **立即修改管理员密码**：默认 `admin / admin123` 仅用于首次体验。本地测试后，可在数据库中删除 `admins` 表的记录，再手动插入新账号；也可以让我帮您加一个「修改密码」功能。
2. `.env` 包含数据库密码，**不要**提交到 GitHub 公开仓库（项目已有 `.gitignore` 忽略它，请勿移除）。
3. 部署到公网时务必启用 HTTPS。
4. 线上支付接入点已预留在 `api/paymentRouter.ts` 与 `.env` 的 `PAYMENT_*` 变量中，接入您选定的支付平台时替换实现即可。

祝使用顺利！如需调整功能或遇到问题，随时回来找我。
